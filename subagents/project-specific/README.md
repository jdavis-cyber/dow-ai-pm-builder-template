# Project-Specific Runtime Packages

Use this tier for language, platform, or workload-specific runtime TOMLs that should activate only when `install-config.json` explicitly selects them.

Do not place regulated overlays here.
